# PostgreSQL connection (matches the docker run command in BACKEND_README.md)
DB_URL=jdbc:postgresql://localhost:5432/archbank
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Secret used to sign JWTs - change this in production
JWT_SECRET=my-secret-key
